# Vite env overrides for Following (Subscriptions) endpoints
# NOTE: Do not include the /api prefix here; axios baseURL already includes /api.
# Example values shown are the spec defaults. Change these to match your backend if needed.

VITE_FOLLOWING_PREFIX=/Following
VITE_FOLLOWING_GET_FOLLOWEES=/Following/getFollowees
VITE_FOLLOWING_FOLLOW=/Following/follow
VITE_FOLLOWING_UNFOLLOW=/Following/unfollow
